function add(numA: number, numB: number): number {
    return numA + numB
}

// const result = add('2', 4); // akan error karena parameter pertama menggunakan string
const result = add(2, 4);
console.log(result)